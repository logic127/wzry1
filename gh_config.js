// ============================================================
//  GitHub 静态托管配置（霜原王者杯）
//  填写后，网页将托管在 GitHub Pages，电脑关机也能访问，
//  管理员通过 GitHub Token 录入数据，实时同步到云端。
//  若以下字段全部留空，则回退到本地 server.py 模式（/api/*）。
// ============================================================
window.GH_CONFIG = {
  // 你的 GitHub 用户名（例如：zhangsan）
  owner: 'logic127',
  // 仓库名（例如：wzry）
  repo: 'wzry1',
  // 分支名（新建仓库默认是 main，旧仓库可能是 master）
  branch: 'main',
  // GitHub Pages 地址，留空则自动推断为 https://<owner>.github.io/<repo>/
  // 例如：https://zhangsan.github.io/wzry/
  pagesUrl: ''
};
